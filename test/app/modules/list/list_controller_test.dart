import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:slimobxl/app/modules/list/list_controller.dart';
import 'package:slimobxl/app/modules/list/list_module.dart';

void main() {
  initModule(ListModule());
  // ListController list;
  //
  setUp(() {
    //     list = ListModule.to.get<ListController>();
  });

  group('ListController Test', () {
    //   test("First Test", () {
    //     expect(list, isInstanceOf<ListController>());
    //   });

    //   test("Set Value", () {
    //     expect(list.value, equals(0));
    //     list.increment();
    //     expect(list.value, equals(1));
    //   });
  });
}
